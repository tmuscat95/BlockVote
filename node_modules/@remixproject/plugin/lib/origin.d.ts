import { PluginDevMode } from "./client";
/** Fetch the default origins for remix */
export declare function getDefaultOrigins(): Promise<any>;
/** Get all the origins */
export declare function getAllOrigins(devMode?: Partial<PluginDevMode>): Promise<string[]>;
/**
 * Check if the sender has the right origin
 * @param origin The origin of the incoming message
 * @param devMode Devmode options
 */
export declare function checkOrigin(origin: string, devMode?: Partial<PluginDevMode>): Promise<boolean>;
